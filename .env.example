# API base (no trailing slash). Omit in production builds to use https://eventsapis.samsarawellness.in
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:4000

# Razorpay Key ID (public — safe in browser). Must match backend RAZORPAY_KEY_ID.
# Also set this in Vercel/hosting — .env.local is not deployed (gitignored).
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_u6ftkngPEG7shl
